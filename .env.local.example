# Portfolio Environment Variables Configuration
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# SANITY CMS CONFIGURATION
# ============================================================================
# Get these from your Sanity project dashboard at https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_sanity_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_READ_TOKEN=your_sanity_read_token_here

# ============================================================================
# RECAPTCHA CONFIGURATION
# ============================================================================
# Get these from Google reCAPTCHA Console: https://www.google.com/recaptcha/admin/create
# Choose reCAPTCHA v2 with "I'm not a robot" checkbox
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# ============================================================================
# EMAIL CONFIGURATION (SMTP)
# ============================================================================

# GMAIL CONFIGURATION (Recommended for personal portfolios)
# Note: Use App Password, not your regular Gmail password
# Generate App Password: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password_here
SMTP_FROM=your_email@gmail.com

# SENDGRID CONFIGURATION (Alternative)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your_sendgrid_api_key_here
# SMTP_FROM=your_verified_sender@yourdomain.com

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================

# 1. SANITY SETUP:
#    - Go to https://sanity.io and create a new project
#    - Copy the Project ID from your project dashboard
#    - Create a read token in the API section
#    - Use 'production' as the dataset name

# 2. RECAPTCHA SETUP:
#    - Visit https://www.google.com/recaptcha/admin/create
#    - Choose reCAPTCHA v2 and "I'm not a robot" checkbox
#    - Add domains: localhost (dev) and your production domain
#    - Copy the Site Key and Secret Key

# 3. EMAIL SETUP:
#    - For Gmail: Enable 2-factor auth and create an App Password
#    - For other providers: Use their SMTP settings
#    - Test your configuration with a simple contact form submission

# 4. ENVIRONMENT VARIABLES:
#    - Never commit .env.local to version control
#    - Use your deployment platform's environment variables for production
#    - Verify all variables are set before running the application
